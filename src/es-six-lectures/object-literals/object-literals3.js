
//example jquery ajax call 
//normally you would write url:url in the function definition but now can write url in es6

/* function saveFile(url,data){
    $.ajax({ method: 'POST', url, data})
}

const url = 'http://goose'
const data = {color: 'red'}

saveFile(url,data) */